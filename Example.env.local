AUTH_SECRET=ランダムな32〜64文字
AUTH_GOOGLE_ID=（GCPで発行したクライアントID）
AUTH_GOOGLE_SECRET=（GCPで発行したクライアントシークレット）
NEXTAUTH_URL=http://localhost:3000
DATABASE_URL=postgresql://user:password@localhost:5432/figfingers
# 拡張の ID は chrome://extensions（デベロッパーモード）で確認できる。
# 拡張のクリップ同期・トークン更新は background service worker から
# Origin: chrome-extension://<ID> で届くため、ここに登録しないと 403 になる。
CLIP_API_ALLOWED_ORIGINS=http://localhost:3000,chrome-extension://your-extension-id

# DB SSH トンネル（npm run dev で自動起動）
# DATABASE_URL が localhost を指している場合は必須
# ローカルに直接 PostgreSQL を立てている場合は不要（空欄でスキップされる）
# ローカル DB がある場合は npm run dev:local でトンネルをスキップできる
DB_SSH_USER=user@your-db-server.example.com
DB_SSH_PORT=22

# 以下はデフォルト値のまま使う場合は省略可
# DB_TUNNEL_LOCAL_PORT=5432          # ローカルで受け付けるポート
# DB_TUNNEL_REMOTE_HOST=localhost    # リモートサーバー上の転送先ホスト
# DB_TUNNEL_REMOTE_PORT=5432         # リモートサーバー上の転送先ポート
# DB_SSH_KEY_PATH=~/.ssh/id_rsa      # SSH 秘密鍵のパス（~ はスクリプト側で展開される）
